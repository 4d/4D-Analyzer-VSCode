name: publish_workflow

on:
  push:
    branches:
      - develop

jobs:
  call-build:
    uses: ./.github/workflows/build.yml
  call-publish:
    needs: call-build
    uses: ./.github/workflows/publish.yml